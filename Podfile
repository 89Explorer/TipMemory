# Uncomment the next line to define a global platform for your project
# platform :ios, '16.6'

target 'TipMemory' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'DGCharts'
  # Pods for TipMemory
  
  target 'TipMemoryTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TipMemoryUITests' do
    # Pods for testing
  end

end
